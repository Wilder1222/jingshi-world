# 项目维护约定

- 只保留现行内容：每次迭代原地更新现有文件，不建立历史版本副本、快照、备份目录或“修订前”文件。
- 历史变化只写根目录 `CHANGELOG.md`，按日期说明新增、调整、删除和验证结果；不要把完整旧稿复制进日志。
- 替代旧文件时同步清理旧内容、旧引用、源登记和生成工具；在删除前核验精确范围，不删除仍有效的独立专题、原始参考资料或用户媒体。
- README、讨论状态与核验记录只呈现当前状态，不叠加历史轮次；当前版本号可以保留，不能仅因版本号较小就误删仍有效的专题。
- Git 提交历史不属于工作区历史副本，不重写 Git 历史；提交、推送仍需用户明确要求。
- C／S／P、场次与任务编号保持稳定；停用编号仅保留最小占位与禁用原因，不保留旧剧情，禁止编号复用。
- 修改源文件后运行资料、图谱与生产包的构建和检查；修改前三集生产规则时执行相应回归测试。
